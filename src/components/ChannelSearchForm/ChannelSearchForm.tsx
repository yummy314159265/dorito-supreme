import { FC } from "react";
import { useForm } from "react-hook-form";

export const ChannelSearchForm: FC = () => {
  const {
    register,
    handleSubmit,
    formState: { errors }
  } = useForm();


  return <div></div>;
};
